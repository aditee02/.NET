class Program{
  static void Main(){
    //create object of ArrayList class
    ArrayList arrayList = new ArrayList(){100,'A'};

    Sample s = new Sample();

    //Add()
    arrayList.Add(s.GetNumber());
    arrayList.Add(s.GetAnotherNumber());
    arrayLst.Add(s..GetMessage());
    arrayList.Add(s.GetEmployee());

    //foreach
    foreach(var item in arrayList){
      Console.WriteLine(item);
    }
    Console.Readkey();
  }
}

class Program{
  static void Main(){
    //create object of ArrayList class
    ArrayList arrayList = new ArrayList(){100,'A'};

    Sample s = new Sample();

    //Add
    arrayList.Add(s.GetNumber());
    arrayList.Add(s.GetAnotherNumber());
    arrayList.Add(s.GetMessege());
    arrayList.Add(s.GetEmployee());

    //foreach
    foreach(var item in arrayList){
      Console.WriteLine(item);
    }

    Console.Readkey();
  }
}

class Program{
  static void Main(){
    //create object of ArrayList 
    ArrayList arrayList = new ArrayList(){100,'A'};
    Sample s = new Sample();

    //Add
    arrayList.Add(s.GetNumber());
    arrayList.Add(s.GetAnotherNumber());
    arrayList.Add(s.GetMessege());
    arrayList.Add(s.GetEmployee());

    //foreach
    foreach(var item in arrayList){
      Console.WriteLine(item);
    }
    Console.ReadLine();
  }
}

class Program{
  static void Main(){
    //create object of ArrayList class
    ArrayList arrayList = new ArrayList(){100,'A'};

    Sample s = new Sample();

    //Add
    arrayList.Add(s.GetNumber());
    arrayList.Add(s.GetAnotherNumber());
    arrayList.Add(s.GetMessage());
    arrayList.Add(s.GetEmployee());

    //foreach
    foreach(var item in arrayList){
      Console.WriteLine(item);
    }

    Console.Readkey();
  }
}

class Program{
  static void Main(){
    //create objet of ArrayList class
    ArrayList arrayList = new ArrayList(){100,'A'};

    Sample s = new Sample();

    //Add
    arraylist.Add(s.GetNumber());
    arrayList.Add(s.GetAnotherNumber());
    arrayList.Add(s.GetMessage());
    arrayList.Add(s.GetEmployee());

    //foreach
    foreach(var item in arrayList){
      Console.Writeline(item);
    }
    Console.Readkey();
  }
}

class Program{
  static void Main(){
    //create object of ArrayList class
    ArrayList arrayList = new ArraList(){100,'A'};

    Sample s = new Sample();

    //Add
    arrayList.Add(s.GetNumber());
    arrayList.Add(s.getAnotherNumber());
    arrayList.Add(s.GetMessege());
    arrayList.Add(s.GetEmployee());

    //foreach
    foreach(var item in arrayList){
      Console.Writeline(item);
    }
    Console.ReadLine();
  }
}